import 'package:get_it/get_it.dart';
import 'package:tenis_app/feature/auth/di/provider_module.dart';



initAuthModule(GetIt sl) async {
  initProvider(sl);
  // initUseCase(sl);
  // initRepository(sl);
  // initDataSource(sl);
}
